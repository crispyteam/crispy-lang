val fib = fun n -> {
    if n < 2 {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
};

val before = clock();
println(fib(30));
val after = clock();
println(str((after - before) / 1000) + "s");